package graph_theory;

enum VertexState { DEFAULT, VISITED,ACTIVE, FRONTIER }
enum EdgeState { DEFAULT,RELAXED,ACTIVE,TREE_EDGE} //Relaxed just means we updated the edge for something like shortest path.





